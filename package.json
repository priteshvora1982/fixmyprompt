{
  "name": "fixmyprompt-extension",
  "version": "0.1.31",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "watch": "node --watch build.js"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
